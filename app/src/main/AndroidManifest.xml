<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.java.messageriedelaterredelouest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">

        <!-- Activité principale : LoginActivity -->
        <activity android:name="app.java.messageriedelaterredelouest.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activité secondaire : MainActivity -->
        <activity android:name="app.java.messageriedelaterredelouest.MainActivity"
            android:exported="false" />
    </application>
</manifest>
